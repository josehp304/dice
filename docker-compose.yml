version: '3.8'
services:
  mongodb:
    image: mongo:7
    container_name: dicebet-mongo
    ports:
      - "27017:27017"
    volumes:
      - dicebet_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: dicebet
    restart: unless-stopped

volumes:
  dicebet_data:
